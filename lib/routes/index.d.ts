/** index.ts (Route)
 *  this file will just collect all of our routes
 *  and export them for easier imports (instead of importing ./routes/user.ts,
 *  we just grab what we need from ./routes)
 */
import User from "./user";
export { User };
